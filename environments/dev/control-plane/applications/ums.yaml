apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name     : cms
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name     : in-cluster
    namespace: control
  source:
    path   : "helm/platform-common-services/control-plane/ums"
    repoURL: "https://github.com/zbykovskyi/argocd.git"
    targetRevision: multienv
    helm:
      valueFiles:
        - /environments/dev/control-plane/applications/ums-values.yaml
      parameters:
        - name: "container.image"
          value: adv4000/app2:v0

  project: default
  syncPolicy:
    automated:
      prune   : true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
